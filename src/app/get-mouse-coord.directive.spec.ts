import { GetMouseCoordDirective } from './get-mouse-coord.directive';

describe('GetMouseCoordDirective', () => {
  it('should create an instance', () => {
    const directive = new GetMouseCoordDirective();
    expect(directive).toBeTruthy();
  });
});
